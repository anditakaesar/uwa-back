{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Uwa-back Debug",
            "type": "go",
            "request": "launch",
            "mode": "debug",
            "program": "${workspaceFolder}",
            "preLaunchTask": "buildenv",
            "envFile": "${workspaceFolder}/.env",
        }
    ]
}